# Mobile may contain ONLY public configuration.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8787

# Never expose AI/data-provider/service secrets with EXPO_PUBLIC_.
# Server-only variables belong in the future server environment.
